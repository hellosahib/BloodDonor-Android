<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.rtsproduction.yef">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC9et1kv07D-FYGqcTrNX99YoPP5aURuYE"/>
        <activity
            android:name=".AlertActivity"
            android:parentActivityName=".DonatorActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".DonatorActivity" />
        <activity android:name=".BloodtypeActivity" />
        <activity android:name=".RequestFormActivity"
            android:theme="@style/ToolBar"
            android:label="Request Form"/>
        <activity android:name=".DetailViewActivity"
            android:theme="@style/ToolBar" />
        <activity android:name=".NotificationDetail"
            android:theme="@style/ToolBar"/>
    </application>

</manifest>